{
  "name": "my-custom-map-fixed",
  "version": "1.0.0",
  "description": "Custom MapLibre basemap: My Custom Map Fixed",
  "type": "module",
  "scripts": {
    "build:styles": "tsx scripts/build-styles.ts",
    "build:styles:local": "SPRITE_CDN=http://localhost:8080 npm run build:styles",
    "build:shields": "tsx scripts/build-shields.ts",
    "build:utils": "tsc --project tsconfig.browser.json",
    "build": "npm run build:utils && npm run build:styles",
    "serve": "node serve.js",
    "dev": "npm run build:styles && npm run serve",
    "verify:tilejson": "node scripts/verify-tilejson.mjs",
    "warm:cache": "node scripts/warm-cache.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "maplibre-gl": "^5.13.0",
    "sharp": "^0.34.5",
    "tsx": "^4.6.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
